{
  "name": "korea",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd \"ajil korea\" && npm install --include=dev && npm run build && cp -r .next ../",
    "dev": "cd \"ajil korea\" && npm run dev",
    "start": "cd \"ajil korea\" && npm start"
  },
  "dependencies": {
    "next": "14.0.4"
  }
}
